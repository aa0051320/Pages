title: "Crescent's Pages"

collections:
  pages:
    output: true

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      output: true
      permalink: /:name
